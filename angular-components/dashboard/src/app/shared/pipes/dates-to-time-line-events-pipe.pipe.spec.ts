import { DatesToTimeLineEventsPipePipe } from './dates-to-time-line-events-pipe.pipe';

describe('DatesToTimeLineEventsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DatesToTimeLineEventsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
