/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RetentionPeriodForFullCalendarPipe } from './retention-Period-For-FullCalendar.pipe';

describe('Pipe: RetentionPeriodForFullCalendare', () => {
  it('create an instance', () => {
    let pipe = new RetentionPeriodForFullCalendarPipe();
    expect(pipe).toBeTruthy();
  });
});
